
  name: CI

  on:
    push:
      branches: [ "main" ]
    pull_request:
      branches: [ "main" ]

    jobs:
      build:
        runs-on: ubuntu-latest

        steps:
          - uses: actions/checkout@v4

          - name: First job
            run: |
              echo Hello, world!,
              echo Testing Testing!
    
